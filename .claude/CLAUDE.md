# CLAUDE.md

<!-- Add your custom instructions below. Repowise will never modify anything outside the REPOWISE markers. -->
<!-- Examples: coding style rules, test commands, workflow preferences, constraints -->

<!-- REPOWISE:START — Do not edit below this line. Auto-generated by Repowise. -->

## Codebase Intelligence for keeppeek2 (Repowise)

Indexed by [Repowise](https://repowise.dev). Last indexed: 2026-08-28 (commit 081975b). Confidence: 100%.

### How to work in this repo

- **Trust the index.** `verified: true` means the bytes were checked against the live tree, so never re-read those lines. Re-read only on `bounds: "approximate"`, `_meta.stale_warning`, `search_method: "bm25"` or `confidence: "low"`; `index_behind: true` alone is informational.
- **Pre-edit, not instead-of-edit.** These tools decide _which_ files to read and edit. Claude Code requires a raw Read of any file you will Edit, and that Read is correct and expected.
- **Noisy commands** (tests, builds, `git log`/`diff`, searches, listings): prefer `repowise distill <cmd>` — same command, exit code preserved, errors-first output. A `[repowise#<ref>: N lines omitted]` marker is recoverable via `repowise expand <ref>` (add `-q <regex>` to filter); never re-run the command to see omitted output.
- **Recording a decision** you had to reason out: `repowise decision add --title T --decision D` records it without prompting and prints the id (`--format json` to parse it back). It lands `proposed`, for a person to confirm.

### Tools

| Tool                                                       | When and why                                                                                                                                                                                                          |
| ---------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `get_answer(question)`                                     | First call for any how/where/why question. Cite `confidence: "high"` or `grounding: "extracted"` directly; `degraded` means judge by `retrieval_quality`. `symbol_bodies` has live bodies.                            |
| `get_context(targets=[...])`                               | Triage card for files/modules/symbols: docs, signatures, hotspot, fix history. No source bytes — `include=["skeleton"]` for the whole file verified; use `include=["callers", "decisions"]` for depth. Batch targets. |
| `get_symbol(id)`                                           | **Follow-up, not an entry point** — one verified body for an id a prior response named (`path.py::Name`, `path.py:140-180`, `repowise#<hex>`). Never walk a file symbol by symbol; Read it.                           |
| `search_codebase(query)`                                   | Hybrid search, auto-routed by query shape; force with `mode=symbol`, `mode=path`, `mode=concept`, or `mode=hybrid`. A hit whose `sources` are `[fts]` only has no semantic agreement, so verify it.                   |
| `get_why(query, targets?)`                                 | Why the code is shaped this way: decision records, git archaeology, rationale comments. Call before a refactor or a pattern divergence.                                                                               |
| `get_risk(targets, changed_files?)`                        | What history says about touching these files. PR mode (`changed_files`) leads with a `directive`: read `will_break` / `missing_cochanges` / `missing_tests` / `tests_to_run` first.                                   |
| `get_change_risk(revspec, extensions?, exclude_patterns?)` | Defect score for a whole commit or `base..head` range, from its diff on the live checkout. Lead with `risk_percentile`. Scores a range; `get_risk` scores paths.                                                      |
| `get_health(targets?, include?)`                           | Defect / maintainability / performance scores and findings. Self-check the files you touched before finishing.                                                                                                        |
| `get_dead_code()`                                          | Confidence-tiered unreachable files / unused exports / zombie packages. For cleanup sweeps, not targeted fixes.                                                                                                       |
| `get_overview()`                                           | Architecture map. Call once, first, in an unfamiliar repo; skip it after that.                                                                                                                                        |

### Architecture

**Files:** 1042 | **Lines:** 248887 | **Monorepo:** 11 packages | **Import cycles:** 14
keeppeek2 is a multi-package rust codebase of 1042 files, split across 11 packages. Execution starts at src/app.rs, src/server.rs, crates/retina/src/server.rs and 70 other entry points. Start here when reading the codebase. Ranked by PageRank over the import graph: the files most of the codebase ultimately depends on.

### Key modules

- `ui/src` — ui/src/lib · ui/src/lib/components · ui/src/lib/components/logging · ui/src/lib/components/ui/badge · ui/src/lib/components/ui/button ·…
- `ui/src/lib` — ui/src/lib · ui/src/lib/components · ui/src/lib/components/logging · ui/src/lib/components/ui/badge · ui/src/lib/components/ui/button ·…
- `ui/visual-harness/stories` — ui/visual-harness/stories
  **Language:** svelte | **Files:** 72 | **Public symbols:** 133 / 323
  Covers the 72 source files in…
- `ui/src/lib/components` — ui/src/lib/components
  **Language:** svelte | **Files:** 61 | **Public symbols:** 64 / 784
  Covers the 61 source files in…
- `ui/src/lib/components/ui` — ui/src/lib/components/ui/badge · ui/src/lib/components/ui/button · ui/src/lib/components/ui/card · ui/src/lib/components/ui/input ·…
- `crates/mp4/src/mp4box` — crates/mp4/src/mp4box
  **Language:** rust | **Files:** 44 | **Public symbols:** 528 / 1234
  Covers the 44 source files in…
- `crates/onvif/xsd-parser/src/parser` — crates/onvif/xsd-parser/src/parser
  **Language:** rust | **Files:** 24 | **Public symbols:** 125 / 279
  Covers the 24 source files in…
- `crates/reo-proto/src` — crates/reo-proto/src
  **Language:** rust | **Files:** 20 | **Public symbols:** 658 / 1798
  Covers the 20 source files in crates/reo-proto/src
- `src/storage` — src/storage
  **Language:** rust | **Files:** 21 | **Public symbols:** 506 / 1189
  Covers the 21 source files in src/storage
- `crates/onvif/xsd-types/src/types` — crates/onvif/xsd-types/src/types · crates/onvif/xsd-types/src/utils
  **Language:** rust | **Files:** 21 | **Public symbols:** 135 /…

### Entry points

- `src/server.rs`
- `src/app.rs`
- `crates/retina/src/server.rs`
- `src/bin/keeppeek_camera/main.rs`
- `ui/visual-harness/.storybook/main.ts`
- `src/lib.rs`

### Files that need care (bug-fix history first, then churn — check `get_risk` before editing)

- `ui/demo/nine-camera-live.demo.ts` — 5 bug fixes, last fix yesterday (bug magnet); 7 commits/90d
- `ui/e2e/peek-live.e2e.ts` — 4 bug fixes, last fix 3 days ago (bug magnet); 9 commits/90d
- `ui/scripts/check-demo-scenarios.ts` — 3 bug fixes, last fix yesterday; 7 commits/90d
- `ui/src/lib/components/LiveVideo.svelte` — 3 bug fixes, last fix 4 days ago; 10 commits/90d
- `src/server.rs` — 3 bug fixes, last fix 4 days ago; 27 commits/90d

### Code health

Three co-equal signals: defect risk 6.63/10 avg, hotspot health 4.38/10 (stable), worst `ui/src/routes/events/+page.svelte` at 1.0/10 · maintainability 6.9/10 · performance risk 288 open static I/O-in-loop / N+1 findings. Detail: `get_health()`.

Critical files:

- `ui/demo/camera-lifecycle.demo.ts` — change entropy — impact −2.8
- `ui/src/lib/components/PeekCameraTile.svelte` — untested hotspot — impact −2.0
- `ui/src/lib/components/LiveVideo.svelte` — untested hotspot — impact −2.0
- `ui/src/lib/types.ts` — untested hotspot — impact −2.0
- `crates/mp4/src/types.rs` — untested hotspot — impact −2.0

<!-- REPOWISE:END -->
