[package]
name = "xop"
version = "0.1.0"
edition = "2024"
license = "MIT"

[package.metadata.cargo-machete]
ignored = ["yaserde"]

[lints]
workspace = true

[dependencies]
validate = { path = "../../validate" }
yaserde = "0.12"
yaserde_derive = "0.12"
