# Azure OpenAI resource base URL and the deployed TTS model name.
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_TTS_DEPLOYMENT=

# Set exactly one locally. Store API keys in GitHub Actions secrets or use a short-lived Entra token.
AZURE_OPENAI_API_KEY=
AZURE_OPENAI_AUTH_TOKEN=